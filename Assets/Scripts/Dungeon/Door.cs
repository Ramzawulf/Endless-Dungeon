using UnityEngine;

namespace Assets.Scripts
{
    public class Door : MonoBehaviour
    {
        //private bool _success = false;

        //public Texture FullScreenTexture;
        //private Rect _fullScreen;
        //public Texture NextLevelTexture;
        //private Rect _nextLevelButton;



        //// Use this for initialization
        //public void Start ()
        //{
        //    var width = Screen.width;
        //    var height = Screen.height;
        //    _fullScreen = new Rect (0, 0, width, height);
        //    _nextLevelButton = new Rect (width / 2 - NextLevelTexture.width / 2, 
        //        height / 2 - NextLevelTexture.height - 2,
        //        NextLevelTexture.width, NextLevelTexture.height);

        //}

        //// Update is called once per frame
        //public void Update ()
        //{
	
        //}

        //public void OnTriggerEnter (Collider col)
        //{
        //    if (col.gameObject.tag != "Player") return;
        //    _success = true;
        //    col.gameObject.GetComponent<HeroBehaviour> ().pause ();
        //}

        //public void OnCollisionEnter (Collision col)
        //{
        //    if (col.gameObject.tag == "Player") {
        //        _success = true;
        //    }
        //}

        ////ToDo Event when exit was reached

    }
}
